# Verify package dependencies

Verify package dependencies

## Usage

``` r
verificar_y_configurar_entorno(instalar = FALSE)
```

## Arguments

- instalar:

  Whether to install missing dependencies explicitly.

## Value

`TRUE`, invisibly, when all dependencies are available.
